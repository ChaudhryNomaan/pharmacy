import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pharmacy.app',
  appName: 'My-pharmacy',
  webDir: 'out' // Change this from 'www' to 'out'
};

export default config;